# Next-Session Target — set 2026-08-25 (supersedes 2026-07-11)

*The standing pointer. If priorities shift between sessions, amend **this** file, not the handoff.*

The 2026-08 sessions were celebration work (birthday artwork #258, invite video #259, both in `meridian/`). The **product ladder from 2026-07-11 stands unchanged** — nothing merged since touched `split/` — so this target carries it forward verbatim and folds in the new carry-forwards.

## The recommended next move
**Begin Phase 1 — Foundations: the two-layer benchmark first** (it makes every later phase measurable), then the chart rework + Chart.js vendoring. Rationale unchanged from 2026-07-11: cheap confidence bought before the expensive builds; the benchmark is the instrument that scores everything after it.

**Time-sensitive exception:** if the Architect opens the next session before **4 September 2026** wanting party-collateral changes (real venue/time on the backdrop foot line; extra pieces), do that first — it is a `PARTY` block edit in `meridian/birthday-one/build-posters.mjs` + rebuild, minutes not hours, outside the Capital.

## Priority ladder (each startable cold — carried from 2026-07-11)
- **P0 — Two-layer benchmark.** Objective gate (`split/audit-benchmark.sh` wired into `build.sh` beside the other ship-gates) **+** advisory Governor rubric (`docs/SPROUTLAB_SCORECARD.md`). Gate: tooling (Public Works) + a `tests/e2e/*.spec.ts`.
- **P1 — Chart rework + vendor Chart.js.** Closes ASI04 (unpinned CDN → local bundle) + Vela's smoothness doctrine. Gate: shared modules → quad-Governor.
- **P2 — Evolving-thesis groundwork.** AoA engine upgrade (first-emergence date as first-class milestone field) → i18n framework (foundation for Hindi).
- **P3 — The content surface (blog).** Human deps: named pediatrician + content cadence.

## Carry-forward register (grouped)

**Human-only / Architect gates**
- **Backdrop logistics line** is still the placeholder ("Friday, 4 September 2026 · Jamshedpur", no time) — needs the real venue/time from the Architect before the flex print, if it should change.
- **Codex structured-records snippet** — still pending Architect nod on canon IDs (from 2026-07-11).
- **Book X "The Borders"** — HELD; do **not** draft the charter unbidden.
- **Drone** — FPV direction locked pending the Architect's FPV-vs-GPS cross-check.

**Successors (in-flight next phases)**
- Phase 1 → Phase 4 (Borders ratification + RED fixes) when unheld.
- Product roadmap: **F-6b** voice-to-text spike, **F-4** Patterns sub-tab, **F-5** `parseFeeding` completion.

**Test / data debt**
- Benchmark remains greenfield (P0 builds it).
- **3 AA-contrast advisories** (pre-existing) in the doctor-card PRINT palette flagged by the build's contrast audit (`--doc-placeholder on --doc-paper` 3.29:1 and two siblings) — triage whether they render only as large/bold (AA ≥ 3.0 passes in context) or need token nudges. Maren-primary consult (doctor card is Care surface).

**Housekeeping**
- **PR-dashboard curation now two batches due:** append **#250–#259** records to `docs/pr-dashboard-data.json` (incl. #258 artwork + #259 video; category taxonomy in `split/pr-dashboard-template.html`). `build-safe.sh` will keep warning until done.
- **Stale remote branch:** `claude/ziva-first-birthday-art-ekh100` on origin (its PRs merged) — prune after this close lands.
- **`meridian/` naming:** `birthday-one/` (#258) and `first-birthday/` (#259) are siblings from parallel sessions — consider unifying naming/README cross-links in a small docs pass.
- **F-6a (#253)** never received its own session-close — do it with context, not blindly (inherited).

**Candidate Codex-canon**
- `compass-vs-north-star` + `north-star-amendable-between-sessions` (inherited, unratified).
- Codex canon-reconciliation: Cipher + Chronicler compass bearings on SproutLab mirrors → reconcile to Codex (canon-cc-026).
- **New candidate — the meridian print pipeline** (pattern record: `meridian/birthday-one/README.md`): vector-SVG keepsake → subset/embedded fonts → measured typesetting from real glyph metrics → seeded procedural texture → headless-Chromium proof PNG + exact-MediaBox PDF. Reusable for future keepsakes (milestone posters, birth-sky prints); a chronicle/doctrine entry when Codex is next in scope.

*— Lyra. The pointer stands: measure first, then polish — and get the venue line right before the flex goes to print.*
